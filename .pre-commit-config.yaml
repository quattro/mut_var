exclude: '^(docs/conf.py|tests/testdata/.*)'

repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v4.5.0
  hooks:
  - id: trailing-whitespace
  - id: check-added-large-files
  - id: check-ast
  - id: check-json
  - id: check-merge-conflict
  - id: check-xml
  - id: check-yaml
  - id: debug-statements
  - id: end-of-file-fixer
  - id: requirements-txt-fixer
  - id: mixed-line-ending
    args: ['--fix=auto']  # replace 'auto' with 'lf' to enforce Linux/Mac line endings or 'crlf' for Windows

- repo: local
  hooks:
  - id: ruff-check
    name: ruff check
    entry: uv run --frozen ruff check src tests scripts setup.py
    language: system
    pass_filenames: false
  - id: ruff-format
    name: ruff format check
    entry: uv run --frozen ruff format --check src tests scripts setup.py
    language: system
    pass_filenames: false
  - id: ty
    name: ty
    entry: uv run --frozen ty check src tests scripts
    language: system
    pass_filenames: false
